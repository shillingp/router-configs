# router-configs

Repository for router-related configuration snippets and lists.

Whitelist (domains/whitelist.txt)

Raw file URL:
https://raw.githubusercontent.com/shillingp/router-configs/main/domains/whitelist.txt

Notes

- The whitelist is one domain per line. Lines beginning with `#` are treated as comments.
- Do not include protocols (http:// or https://) or paths.
